<UserControl x:Class="CyTool.Views.AnalyseNetworkControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#FF2C2E33">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
            <TextBlock Text="Network Analysis Tool"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            <Button Content="Scan Network"
        Width="212"
        Margin="10"
        Command="{Binding ScanNetworkCommand}"
        Height="24"
        Style="{StaticResource CyberButton}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>


            <TextBlock Text="{Binding Status}"
                       FontSize="16"
                       Foreground="LightGray"
                       HorizontalAlignment="Center"
                       Margin="10"/>
            <ListView ItemsSource="{Binding Devices}" Height="200" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="IP Address" DisplayMemberBinding="{Binding IPAddress}" Width="150"/>
                        <GridViewColumn Header="Host Name" DisplayMemberBinding="{Binding name}" Width="150"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Local IP:" 
                           FontSize="18" 
                           Foreground="White" 
                           Margin="10" Width="69"/>
                <TextBlock Text="{Binding LocalIp}"
                           FontSize="18"
                           Foreground="LightGray"
                           TextWrapping="Wrap" Width="383" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
