<UserControl x:Class="CyTool.Views.AnalyseNetworkControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#FF2C2E33">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
            <TextBlock Text="Network Analysis Tool"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            <Button Content="Scan Network"
                    Width="150"
                    Margin="10"
                    Command="{Binding ScanNetworkCommand}"/>
            <TextBlock Text="{Binding Status}"
                       FontSize="16"
                       Foreground="LightGray"
                       HorizontalAlignment="Center"
                       Margin="10"/>
            <ListView ItemsSource="{Binding Devices}" Height="200" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="IP Address" DisplayMemberBinding="{Binding IPAddress}" Width="150"/>
                        <GridViewColumn Header="MAC Address" DisplayMemberBinding="{Binding MacAddress}" Width="150"/>
                        <GridViewColumn Header="Host Name" DisplayMemberBinding="{Binding HostName}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <TextBlock Text="Additional Network Info:" 
                       FontSize="18" 
                       Foreground="White" 
                       Margin="10"/>
            <ScrollViewer Height="150" Margin="10">
                <TextBlock Text="{Binding AdditionalInfo}"
                           FontSize="14"
                           Foreground="LightGray"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
