<UserControl x:Class="CyTool.Views.DirectoryBruteforceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FF202124" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Directory Enumeration Tool" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="White" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20" />

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
            <TextBlock Text="Base URL:" Foreground="White" FontSize="16"/>
            <TextBox Text="{Binding BaseUrl, UpdateSourceTrigger=PropertyChanged}" 
                     Width="400" Height="30" 
                     Margin="0,5,0,15" 
                     Padding="5"
                     Background="#FF2A2D31" 
                     Foreground="White" 
                     BorderBrush="Gray" 
                     BorderThickness="1" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button Content="Start Enumeration"
                    Style="{StaticResource CyberButton}"
                    Command="{Binding StartEnumerationCommand}"
                    Width="231" 
                    Margin="0,0,10,0" />
            <Button Content="Stop"
                    Style="{StaticResource CyberButton}"
                    Command="{Binding StopEnumerationCommand}"
                    Width="122" />
            <Button Content="Export Output"
                    Style="{StaticResource CyberButton}"
                    Command="{Binding ExportOutputCommand}"
                    Width="185"
                    Margin="10,0,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center">
            <TextBlock Text="Output:" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       Margin="0,0,0,5" 
                       FontSize="16" />
            <TextBox Text="{Binding OutputLog, UpdateSourceTrigger=PropertyChanged}"
                     Width="500" Height="200" 
                     IsReadOnly="True" 
                     VerticalScrollBarVisibility="Auto" 
                     TextWrapping="Wrap"
                     Background="#FF2A2D31"
                     Foreground="White"
                     BorderBrush="Gray"
                     BorderThickness="1"
                     Padding="5"/>
        </StackPanel>
    </Grid>
</UserControl>